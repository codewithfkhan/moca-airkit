# AIR MOCA SDK Configuration
# These values should be obtained from your Moca Network Dashboard
# https://developers.sandbox.air3.com/

# Copy this file to .env and fill in your actual values

# Backend Configuration (JWT token generation)
VITE_BACKEND_URL=http://localhost:8080

# AIR SDK Configuration
VITE_PROGRAM_ID=your_program_id_here
VITE_PARTNER_ID=your_partner_id_here
VITE_REDIRECT_URL=https://developers.sandbox.air3.com/example/issue

# Notes:
# - API URL is automatically determined by BUILD_ENV (SANDBOX/PRODUCTION) in code
# - JWT tokens are generated dynamically by the backend server (no static API key needed)
